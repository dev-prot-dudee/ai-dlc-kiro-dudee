<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations
- 2026-08-20T07:30:00Z — ตีความว่า integration test ไม่มีความหมายในรุ่นนี้; Minimal strategy ไม่กำหนดไว้ และรุ่นนี้ไม่มีขอบเขตระหว่างระบบให้ integrate เลย (ไม่มี backend ไม่มี API ไม่มี service ภายนอก) การเชื่อมต่อเดียวที่มีคือ localStorage ซึ่ง storage.spec.ts ครอบไว้แล้ว 7 scenario จึงไม่สร้างไฟล์ทั้งที่ produces ระบุชื่อไว้
- 2026-08-20T07:30:00Z — ตีความว่า security test ไม่มีอะไรให้ทดสอบ; requirements.md ไม่มี NFR ด้านความปลอดภัยเพราะตัด auth ออกโดยเจตนาที่ requirements-analysis ข้อ 4 ไม่มี endpoint ไม่มี input จากภายนอก ไม่มี secret ในโค้ด แต่บันทึกไว้ชัดว่า "ไม่มีอะไรให้ทดสอบ" ไม่เท่ากับ "ปลอดภัย" — แปลว่าไม่มีการควบคุมการเข้าถึงเลย
- 2026-08-20T07:30:00Z — ตีความชื่อไฟล์ผลลัพธ์ว่าใช้ `build-test-results.md` ตามที่ engine ระบุใน produces ไม่ใช่ `test-results.md` ตามที่ stage prose ขั้นที่ 10 เขียน; สองที่ไม่ตรงกัน เลือกตาม engine เพราะ sensor ตรวจตามรายการ produces

## Deviations
- 2026-08-20T07:30:00Z — สร้าง `performance-test-instructions.md` และวัดผลจริงทั้งที่ Test Strategy เป็น Minimal ซึ่งกำหนดว่าไม่ต้องสร้างไฟล์ test instruction เพิ่ม; stage prose เปิดช่องไว้ว่าสร้าง test type เพิ่มได้ทุกระดับ strategy ถ้าบริบทเรียกร้อง และบริบทเรียกร้องชัดเจนเพราะ code-summary.md ระบุว่า NFR2 กับ NFR3 ยังไม่ได้วัด ทั้งสองข้อมีเกณฑ์ที่วัดได้ การปล่อยผ่านโดยไม่วัดขัดกับข้อกำหนด inception ที่ทุก requirement ต้องมีเกณฑ์ผ่าน/ไม่ผ่านที่ตรวจได้
- 2026-08-20T07:30:00Z — แยกไฟล์วัดผลไว้ที่ `src/perf/` และไม่รวมใน `npm test`; การวัดเวลาแปรผันตามเครื่องและภาระงานขณะนั้น ถ้ารวมใน test ที่ใช้เป็นเกณฑ์ merge จะได้ CI ที่ล้มเป็นครั้งคราวโดยที่โค้ดไม่ได้ผิด เพิ่ม script `test:perf` แยกให้เรียกได้ชัดเจน

## Tradeoffs
- 2026-08-20T07:30:00Z — เลือกวัดบน jsdom แทนการติดตั้ง Playwright หรือ Lighthouse; jsdom วัด layout กับ paint ไม่ได้จึงได้แค่ขอบล่างของเวลาจริง แต่ตอบคำถามที่เสี่ยงที่สุดได้ — มีอัลกอริทึมที่ระเบิดตามจำนวนข้อมูลหรือไม่ การติดตั้ง browser automation ใช้เวลาดาวน์โหลดมากและผู้ใช้มีเวลาจำกัด จึงบันทึกข้อจำกัดไว้ชัดแล้วส่งต่อการวัดในเบราว์เซอร์จริงเป็นงานมือ
- 2026-08-20T07:30:00Z — เลือกไม่ติดตั้ง Lighthouse เอง; เป็นการเพิ่ม dependency ขนาดใหญ่ที่ผู้ใช้ไม่ได้ขอ และ NFR1 วัดได้เฉพาะในเบราว์เซอร์จริงซึ่งต้องมีคนดูผลอยู่แล้ว จึงเขียนคำสั่งไว้ให้พร้อมใช้แทนการติดตั้งล่วงหน้า
- 2026-08-20T07:30:00Z — ตั้งเกณฑ์ขนาดข้อมูลไว้ที่ 2.5 MB แทนเพดานจริง ~5 MB ของเบราว์เซอร์; เหลือที่ครึ่งหนึ่งให้ field ที่จะเพิ่มในรุ่นถัดไป (evidence, dual estimate, NFR metric) แลกกับการที่เกณฑ์เข้มกว่าที่ NFR3 กำหนดจริง

## Open questions
- 2026-08-20T07:30:00Z — การ render board ใช้ 67-87 ms คือราว 40% ของงบ 200 ms และยังไม่รวม paint; ถ้าข้อมูลจริงโตเกิน 500 รายการต่อ entity จะเกินงบแน่ ควรตัดสินใจเรื่อง virtual scrolling หรือแบ่งหน้าก่อนที่ข้อมูลจะโต ไม่ใช่หลังจากผู้ใช้บ่นว่าช้า
- 2026-08-20T07:30:00Z — NFR1 กับ NFR4 ยังไม่มีตัวเลขยืนยันแม้แต่ตัวเดียวและผมทำแทนไม่ได้จากที่นี่; ต้องยืนยันว่าใครจะทำและทำเมื่อไรก่อน deploy ให้คนอื่นใช้ โดยเฉพาะ Safari ที่จำกัด localStorage เข้มกว่าที่อื่นและระบบนี้พึ่ง localStorage ทั้งหมด
- 2026-08-20T07:30:00Z — พบ test ที่ผ่านโดยไม่ได้ทดสอบอะไรสองกรณีในขั้นนี้ (board render 0 การ์ด, traceForward ที่ไม่เคย match); ทั้งสองกรณีจับได้เพราะเอะใจกับตัวเลขที่ดีเกินคาด ไม่ใช่เพราะเครื่องมือเตือน ควรพิจารณาว่ามี sensor หรือกฎที่บังคับให้ทุกการวัดต้องมี assertion ยืนยันว่างานเกิดขึ้นจริงหรือไม่
